# Testing Checklist (short)

- Run `npm run build` after code edits.
- Run `npm run dev` for local verification of runtime behavior.
- Reproduce user-reported steps (1-3 actions) and capture console errors.
- If a unit test exists, run it. Otherwise, validate the specific page/route.
